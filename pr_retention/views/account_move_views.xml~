<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_account_move_form_retention" model="ir.ui.view">
        <field name="name">account.move.form.retention</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <div class="oe_stat_button"
                     attrs="{'invisible': ['|', ('move_type', 'not in', ('out_invoice', 'out_refund')), ('retention_amount', '=', 0)]}">
                    <field name="retention_amount" widget="statinfo" string="Retention"/>
                </div>
            </xpath>
            <xpath expr="//field[@name='invoice_payment_term_id']" position="after">
                <field name="retention_amount"
                       readonly="1"
                       attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))]}"/>
            </xpath>
        </field>
    </record>
</odoo>
