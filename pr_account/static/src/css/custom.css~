
.btn-teal-custom {

  color: #fff !important;
  background-color: #008080 !important;
  border-color: #008080 !important;

}


.btn-teal-custom:hover {
  color: #fff;
  background-color: #0ea9a9;
  border-color: #0ea9a9;
}


/* Scrollable One2many with sticky headers */
.freeze-1many .o_section_and_note_list_view {
    display: block !important;
    max-height: 380px !important;
    overflow-y: auto !important;
    border-collapse: separate !important;
    border-spacing: 0;
    width: 100%;
}

/* Make header sticky without breaking table layout */
.freeze-1many .o_section_and_note_list_view thead th {
    position: sticky !important;
    top: 0 !important;
    background: #f8f9fa !important; /* same as Odoo light gray */
    z-index: 10 !important;
}

/* Keep column alignment consistent */
.freeze-1many .o_section_and_note_list_view th,
.freeze-1many .o_section_and_note_list_view td {
    white-space: nowrap;
    text-align: left;
    padding: 4px 8px;
}

.freeze-1many .o_section_and_note_list_view tbody tr:nth-child(even) {
    background-color: #f9f9f9;
}
